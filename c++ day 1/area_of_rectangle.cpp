//area of rectangle
#include<iostream>
using namespace std;
int main(){
    float length,breath,area;
    cout<<("enter the length of rectangle:");
    cin>>length;
    cout<<("enter the breath of rectangle:");
    cin>>breath;
    area=length*breath;
    cout<<area;
}